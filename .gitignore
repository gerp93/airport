# Godot 4 local cache/import files
.godot/
*.translation

# Local per-machine export output path overrides. export_presets.cfg itself IS
# committed — CI cannot export without it, and this project's presets hold no
# signing credentials. See KVG_Standards' game-repos.md.
export.cfg

# Built artifacts
build/
release_out/
